{
  "name": "duckalization",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
